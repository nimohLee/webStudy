/**
 * 1. 기본 기능 node 설치하면 포함(nodejs.org)
 * 2. 꼭 필요한 기능이 아니면 숨겨두기에 사용 require();
    * fs, path : 내부 모듈이라서 설치없이 사용 가능 ex) require('fs'), require('path');
 * 3. 추가적인(node에 포함 안된 것들) 필수옵션(모듈) npm 설치
 * 4. 대부분의 서버용 'npm i(install) 모듈명' 콘솔에서 설치한 후 사용
    * npmjs.com > 설치할 모듈 검색 > 설치할 수 있는 명령어 나오는 거 콘솔에다가 입력
    * 예를 들어 express 설치 후 const express = require('express'); 이렇게 사용하면됨
 */